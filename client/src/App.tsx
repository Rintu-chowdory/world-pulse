// Meridian Command: the app shell stays minimal so the asymmetric situation-room page owns the visual hierarchy.
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
